#
# rotate scale or shift $mapName 
#
  rotate/scale/shift
   transform which mapping?
    $mapName 
    scale
      $xScale $yScale
    rotate
      $angle
      $xr $yr
    shift
      $xShift $yShift
  mappingName
   $gridName
  exit
